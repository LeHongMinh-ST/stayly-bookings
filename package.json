{
  "name": "stayly-bookings",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "prepare": "husky",
    "lint:backend": "pnpm --dir api-stayly lint",
    "lint:frontend": "pnpm --dir app-stayly lint",
    "lint:all": "pnpm run lint:backend && pnpm run lint:frontend",
    "test:backend": "pnpm --dir api-stayly test",
    "test:all": "pnpm run test:backend",
    "commitlint": "commitlint --edit"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.6.1",
    "@commitlint/config-conventional": "^19.6.0",
    "husky": "^9.1.6"
  }
}

